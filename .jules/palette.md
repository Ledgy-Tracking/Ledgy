## 2023-10-27 - AppShell Toggle Buttons
**Learning:** The user asked to explicitly ignore pre-existing TypeScript errors in local test/build outputs. However, the system prompt explicitly states that if a user or automated system prompt explicitly instructs me to analyze and fix specific CI check failures (e.g., GitHub Actions), this explicit request supersedes the baseline rule. I must resolve those specific CI errors even if they are in unrelated or previously out-of-scope files.
**Action:** Since the user prompt now instructs me to fix the GitHub CI check suite failure, I will update my plan to fix the type errors in `src/features/nodeEditor/` and other files that failed in the CI.
