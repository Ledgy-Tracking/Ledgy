name: Verify Utils

on:
  pull_request:
    branches:
      - main
    paths:
      - 'src/lib/utils.ts'
      - 'src/lib/utils.test.ts'
      - 'package.json'
      - 'package-lock.json'

jobs:
  test:
    name: Run Utils Tests
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run Vitest tests for utils
        run: npx vitest run src/lib/utils.test.ts
