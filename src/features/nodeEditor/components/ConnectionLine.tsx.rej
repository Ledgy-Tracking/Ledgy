--- src/features/nodeEditor/components/ConnectionLine.tsx
+++ src/features/nodeEditor/components/ConnectionLine.tsx
@@ -79,10 +79,10 @@
     fromY,
     toX,
     toY,
-    connectionLineType,
+    connectionLineType: _connectionLineType,
     connectionStatus = 'default',
-    fromNode,
-    fromHandle,
+    fromNode: _fromNode,
+    fromHandle: _fromHandle,
     sourceDirection,
 }) => {
     // Determine control points based on direction for smooth bezier curves
